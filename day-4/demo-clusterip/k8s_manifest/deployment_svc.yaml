apiVersion: v1
kind: Service
metadata:
  name: python-api-svc
spec:
  selector:
    app: python-api     # Matches Deployment labels
  ports:
    - protocol: TCP
      port: 80          # Service port (internal)
      targetPort: 8000  # Container port from your Deployment
  type: ClusterIP       # Default; can omit
